<Project>
  <!-- Recursively import Directory.Build.props in folders above -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('$(MSBuildThisFile)', '$(MSBuildThisFileDirectory)../'))" />
  
  <PropertyGroup>
    <ResoniteProjectType>plugin</ResoniteProjectType>
  </PropertyGroup>
</Project>